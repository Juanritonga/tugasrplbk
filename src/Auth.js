// Auth.js
const Auth = {
    isAuthenticated: () => {
      // Logika autentikasi Anda di sini
      return true; // Contoh: selalu mengembalikan true untuk keperluan demonstrasi
    },
  };
  
  export default Auth;
  